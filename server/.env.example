PORT=3000
JWT_SECRET=change-this-to-a-long-random-string
JWT_EXPIRES_IN=7d
# Comma-separated list of allowed browser origins (the deployed web app's origin)
CORS_ORIGIN=http://localhost:8081,https://khurshidbekurazaliev.github.io
